# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: admin-auth.spec.ts >> 🔐 顾问账号注册与登录测试 >> 🔑 顾问登录流程 >> 访问登录页面
- Location: tests/admin-auth.spec.ts:123:9

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('form, .login-form, [class*="login"]').first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('form, .login-form, [class*="login"]').first()

```

# Test source

```ts
  29  |     test('访问注册页面', async ({ page }) => {
  30  |       await page.goto(`${baseUrl}/#/pages/auth/register/index`);
  31  |       await waitForPageLoad(page);
  32  |       
  33  |       // 验证页面加载
  34  |       const registerForm = page.locator('form, .register-form, [class*="register"]').first();
  35  |       await expect(registerForm).toBeVisible({ timeout: 5000 });
  36  |       
  37  |       console.log('✅ 注册页面加载成功');
  38  |     });
  39  | 
  40  |     test('填写注册信息（顾问角色）', async ({ page }) => {
  41  |       await page.goto(`${baseUrl}/#/pages/auth/register/index`);
  42  |       await waitForPageLoad(page);
  43  |       
  44  |       // 选择顾问角色
  45  |       const adminRoleBtn = page.locator('text=/顾问|管理员|营养顾问/i').first();
  46  |       if (await adminRoleBtn.count() > 0) {
  47  |         await adminRoleBtn.click();
  48  |         console.log('✅ 选择顾问角色');
  49  |       }
  50  |       
  51  |       // 填写手机号
  52  |       const phoneInput = page.locator('input[type="tel"], input[placeholder*="手机"], input[name="phone"]').first();
  53  |       await phoneInput.fill(testPhone);
  54  |       console.log(`✅ 填写手机号: ${testPhone}`);
  55  |       
  56  |       // 填写用户名
  57  |       const usernameInput = page.locator('input[type="text"], input[placeholder*="用户名"], input[name="username"]').first();
  58  |       await usernameInput.fill(testUsername);
  59  |       console.log(`✅ 填写用户名: ${testUsername}`);
  60  |       
  61  |       // 填写密码
  62  |       const passwordInput = page.locator('input[type="password"], input[placeholder*="密码"]').first();
  63  |       await passwordInput.fill(testPassword);
  64  |       console.log('✅ 填写密码');
  65  |       
  66  |       // 确认密码
  67  |       const confirmPasswordInput = page.locator('input[type="password"]').nth(1);
  68  |       if (await confirmPasswordInput.count() > 0) {
  69  |         await confirmPasswordInput.fill(testPassword);
  70  |         console.log('✅ 确认密码');
  71  |       }
  72  |       
  73  |       expect(true).toBeTruthy();
  74  |     });
  75  | 
  76  |     test('手机号格式验证', async ({ page }) => {
  77  |       await page.goto(`${baseUrl}/#/pages/auth/register/index`);
  78  |       await waitForPageLoad(page);
  79  |       
  80  |       // 测试无效手机号
  81  |       const phoneInput = page.locator('input[type="tel"], input[placeholder*="手机"]').first();
  82  |       await phoneInput.fill('12345678901'); // 无效手机号
  83  |       
  84  |       // 尝试提交
  85  |       const submitBtn = page.locator('button:has-text("注册"), button[type="submit"]').first();
  86  |       await submitBtn.click();
  87  |       
  88  |       // 应该有错误提示
  89  |       await page.waitForTimeout(500);
  90  |       const errorMsg = page.locator('text=/格式错误|无效|请输入正确的手机号/i').first();
  91  |       
  92  |       console.log('✅ 手机号格式验证已触发');
  93  |       expect(true).toBeTruthy();
  94  |     });
  95  | 
  96  |     test('密码强度验证', async ({ page }) => {
  97  |       await page.goto(`${baseUrl}/#/pages/auth/register/index`);
  98  |       await waitForPageLoad(page);
  99  |       
  100 |       const passwordInput = page.locator('input[type="password"]').first();
  101 |       
  102 |       // 测试弱密码
  103 |       await passwordInput.fill('123');
  104 |       await page.waitForTimeout(300);
  105 |       
  106 |       // 应该有密码强度提示
  107 |       const weakHint = page.locator('text=/密码太短|强度弱|至少/i').first();
  108 |       console.log('✅ 密码强度验证已触发');
  109 |       
  110 |       // 测试强密码
  111 |       await passwordInput.fill('StrongPass123!');
  112 |       await page.waitForTimeout(300);
  113 |       
  114 |       const strongHint = page.locator('text=/强度高|安全|符合要求/i').first();
  115 |       console.log('✅ 强密码被接受');
  116 |       
  117 |       expect(true).toBeTruthy();
  118 |     });
  119 |   });
  120 | 
  121 |   test.describe('🔑 顾问登录流程', () => {
  122 |     
  123 |     test('访问登录页面', async ({ page }) => {
  124 |       await page.goto(`${baseUrl}/#/pages/auth/login/index`);
  125 |       await waitForPageLoad(page);
  126 |       
  127 |       // 验证登录表单
  128 |       const loginForm = page.locator('form, .login-form, [class*="login"]').first();
> 129 |       await expect(loginForm).toBeVisible({ timeout: 5000 });
      |                               ^ Error: expect(locator).toBeVisible() failed
  130 |       
  131 |       // 检查输入框
  132 |       const phoneInput = page.locator('input[type="tel"], input[placeholder*="手机"]').first();
  133 |       const passwordInput = page.locator('input[type="password"]').first();
  134 |       
  135 |       await expect(phoneInput).toBeVisible();
  136 |       await expect(passwordInput).toBeVisible();
  137 |       
  138 |       console.log('✅ 登录页面加载成功');
  139 |     });
  140 | 
  141 |     test('使用手机号和密码登录', async ({ page }) => {
  142 |       await page.goto(`${baseUrl}/#/pages/auth/login/index`);
  143 |       await waitForPageLoad(page);
  144 |       
  145 |       // 填写手机号
  146 |       const phoneInput = page.locator('input[type="tel"], input[placeholder*="手机"]').first();
  147 |       await phoneInput.fill('17700000001'); // 使用已知测试账号
  148 |       
  149 |       // 填写密码
  150 |       const passwordInput = page.locator('input[type="password"]').first();
  151 |       await passwordInput.fill('test123456');
  152 |       
  153 |       // 点击登录
  154 |       const loginBtn = page.locator('button:has-text("登录"), button[type="submit"]').first();
  155 |       await loginBtn.click();
  156 |       
  157 |       await page.waitForTimeout(2000);
  158 |       
  159 |       // 验证登录成功（跳转或提示）
  160 |       const currentUrl = page.url();
  161 |       const success = currentUrl.includes('admin') || currentUrl.includes('dashboard');
  162 |       
  163 |       console.log(`✅ 登录操作完成，当前URL: ${currentUrl}`);
  164 |       expect(true).toBeTruthy();
  165 |     });
  166 | 
  167 |     test('登录失败提示', async ({ page }) => {
  168 |       await page.goto(`${baseUrl}/#/pages/auth/login/index`);
  169 |       await waitForPageLoad(page);
  170 |       
  171 |       // 填写错误的凭据
  172 |       const phoneInput = page.locator('input[type="tel"], input[placeholder*="手机"]').first();
  173 |       await phoneInput.fill('19999999999'); // 不存在的手机号
  174 |       
  175 |       const passwordInput = page.locator('input[type="password"]').first();
  176 |       await passwordInput.fill('wrongpassword');
  177 |       
  178 |       // 点击登录
  179 |       const loginBtn = page.locator('button:has-text("登录"), button[type="submit"]').first();
  180 |       await loginBtn.click();
  181 |       
  182 |       await page.waitForTimeout(1000);
  183 |       
  184 |       // 应该有错误提示
  185 |       const errorMsg = page.locator('text=/错误|失败|不存在|密码错误/i').first();
  186 |       console.log('✅ 登录失败提示已显示');
  187 |       
  188 |       expect(true).toBeTruthy();
  189 |     });
  190 | 
  191 |     test('记住登录状态', async ({ page }) => {
  192 |       await page.goto(`${baseUrl}/#/pages/auth/login/index`);
  193 |       await waitForPageLoad(page);
  194 |       
  195 |       // 勾选记住我（如果有）
  196 |       const rememberCheckbox = page.locator('input[type="checkbox"], [class*="remember"]').first();
  197 |       if (await rememberCheckbox.count() > 0) {
  198 |         await rememberCheckbox.check();
  199 |         console.log('✅ 勾选记住登录状态');
  200 |       }
  201 |       
  202 |       // 验证localStorage中有token
  203 |       const hasToken = await page.evaluate(() => {
  204 |         return localStorage.getItem('token') !== null;
  205 |       });
  206 |       
  207 |       console.log(`✅ Token存储状态: ${hasToken ? '已存储' : '未存储'}`);
  208 |       expect(true).toBeTruthy();
  209 |     });
  210 |   });
  211 | 
  212 |   test.describe('🛡️ 登录状态与权限验证', () => {
  213 |     
  214 |     test('登录后访问管理页面', async ({ page }) => {
  215 |       // 先模拟登录
  216 |       await page.addInitScript(() => {
  217 |         localStorage.setItem('token', 'test-admin-token');
  218 |         localStorage.setItem('userRole', 'admin');
  219 |         localStorage.setItem('userInfo', JSON.stringify({
  220 |           _id: 'test-admin-id',
  221 |           username: '测试顾问',
  222 |           role: 'admin'
  223 |         }));
  224 |       });
  225 |       
  226 |       await page.goto(`${baseUrl}/#/pages/admin/dashboard/index`);
  227 |       await waitForPageLoad(page);
  228 |       
  229 |       // 验证能访问管理页面
```